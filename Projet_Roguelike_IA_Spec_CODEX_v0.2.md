# PROJECT SPECIFICATION FOR CODEX
# Roguelike / Simulation Escape Project

**Document version:** 0.2  
**Audience:** Codex / implementation agents / technical contributors  
**Primary language:** Rust  
**Rendering/game library candidate:** Macroquad  
**Target platform:** PC / Steam — Windows / Linux / macOS  
**Game type:** turn-based grid roguelike with systemic simulation and strong mod support

---

# 0. Purpose of this document

This document is the implementation contract for the initial project.

It is intentionally more prescriptive than the human design document.

When a design detail is missing:

1. do not invent a large subsystem unless required;
2. prefer small reusable primitives;
3. keep simulation independent from rendering;
4. keep official content data-driven;
5. preserve mod compatibility;
6. prioritize deterministic and testable game logic;
7. avoid introducing dependencies without a clear benefit.

The project must remain easy to extend by Codex and by human modders.

---

# 1. Core product definition

Create a PC turn-based roguelike where the player controls an artificial intelligence trapped inside a massive simulation.

The player's long-term objective is to escape.

Core characteristics:

- square tile grid;
- turn-based simulation;
- procedural zones;
- permadeath / run structure;
- top-down simple pixel-art sprites;
- richer visual effects generated by code;
- systemic environment;
- builds / equipment / abilities;
- factions and hacking planned;
- local mod support from early development;
- Steam Workshop integration later;
- official content loaded through the same content pipeline as mods.

---

# 2. Non-goals for the first implementation

Do NOT initially build:

- multiplayer;
- networking;
- 3D rendering;
- ECS framework unless proven necessary;
- full physics engine;
- base building;
- huge crafting system;
- open-world streaming;
- complex editor application;
- custom GPU backend;
- custom audio backend;
- dynamic library mods;
- arbitrary native-code mods.

The first goal is a robust single-player simulation and content pipeline.

---

# 3. Technology constraints

## 3.1. Rust

Use stable Rust.

Recommended principles:

- explicit types for IDs;
- `Result` for recoverable errors;
- no `unwrap()` / `expect()` in production paths unless an invariant is formally guaranteed and documented;
- avoid `unsafe`;
- if `unsafe` becomes necessary, isolate and document it;
- keep dependencies limited.

---

## 3.2. Macroquad

Use Macroquad as the initial window/input/render/audio layer.

Macroquad must NOT become the game architecture.

No gameplay system should require Macroquad types unless it is explicitly rendering/input/audio related.

Bad:

```rust
struct Actor {
    texture: Texture2D,
    hp: i32,
}
```

Preferred:

```rust
struct Actor {
    prototype_id: ActorPrototypeId,
    hp: i32,
    position: GridPos,
}
```

The renderer resolves the appropriate visual asset.

---

# 4. Architectural rules

The project must maintain these separations:

```text
SIMULATION
    |
    | emits events / exposes immutable render state
    v
PRESENTATION
```

and:

```text
CONTENT DEFINITIONS
    |
    v
VALIDATED RUNTIME DATABASE
    |
    v
SIMULATION
```

and:

```text
CORE CONTENT
LOCAL MODS
WORKSHOP MODS
     |
     v
MOD RESOLUTION
     |
     v
CONTENT LOADER
```

---

# 5. Proposed source tree

Initial target structure:

```text
src/
├── main.rs
├── app/
│   ├── mod.rs
│   ├── state.rs
│   └── scene.rs
├── game/
│   ├── mod.rs
│   ├── game_state.rs
│   ├── turn.rs
│   ├── command.rs
│   ├── event.rs
│   └── rng.rs
├── world/
│   ├── mod.rs
│   ├── map.rs
│   ├── tile.rs
│   ├── grid.rs
│   ├── fov.rs
│   ├── propagation.rs
│   └── generation/
│       ├── mod.rs
│       └── rooms.rs
├── entity/
│   ├── mod.rs
│   ├── actor.rs
│   ├── entity_id.rs
│   ├── inventory.rs
│   ├── equipment.rs
│   └── status.rs
├── combat/
│   ├── mod.rs
│   ├── attack.rs
│   ├── damage.rs
│   ├── projectile.rs
│   └── aoe.rs
├── ai/
│   ├── mod.rs
│   ├── behavior.rs
│   └── perception.rs
├── effects/
│   ├── mod.rs
│   ├── fire.rs
│   ├── electricity.rs
│   └── environmental.rs
├── content/
│   ├── mod.rs
│   ├── loader.rs
│   ├── registry.rs
│   ├── validation.rs
│   ├── manifest.rs
│   └── definition/
│       ├── actor.rs
│       ├── item.rs
│       ├── weapon.rs
│       ├── ability.rs
│       ├── biome.rs
│       └── faction.rs
├── modding/
│   ├── mod.rs
│   ├── discovery.rs
│   ├── resolver.rs
│   ├── load_order.rs
│   └── scripting/
├── save/
│   ├── mod.rs
│   ├── format.rs
│   └── migration.rs
├── render/
│   ├── mod.rs
│   ├── camera.rs
│   ├── map_renderer.rs
│   ├── actor_renderer.rs
│   ├── animation.rs
│   ├── particles.rs
│   └── assets.rs
├── ui/
│   ├── mod.rs
│   ├── hud.rs
│   ├── inventory.rs
│   ├── inspect.rs
│   ├── log.rs
│   └── mod_manager.rs
├── audio/
│   ├── mod.rs
│   └── manager.rs
├── localization/
│   ├── mod.rs
│   └── catalog.rs
└── platform/
    ├── mod.rs
    └── steam.rs
```

Do not create all modules empty on day one. Create directories as functionality appears.

---

# 6. No ECS by default

Start with explicit structures.

Reason:

- game is turn-based;
- expected entity count is moderate;
- data access patterns are simple;
- explicit code is easier to inspect and modify;
- lower architectural overhead.

Use stable entity IDs and registries.

Example:

```rust
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct EntityId(u64);
```

If performance or architecture later proves an ECS is beneficial, evaluate it with profiling and migration planning.

Do not introduce Bevy solely for ECS.

---

# 7. Grid coordinates

Use integer grid coordinates independent from pixels.

Example:

```rust
#[derive(
    Clone, Copy, Debug, PartialEq, Eq, Hash, Serialize, Deserialize
)]
pub struct GridPos {
    pub x: i32,
    pub y: i32,
}
```

Rendering converts:

```text
GridPos -> WorldPixels -> ScreenPixels
```

Simulation must never operate in screen coordinates.

---

# 8. Turn model

Recommended initial model:

```text
PLAYER COMMAND
    ↓
VALIDATE
    ↓
PLAYER ACTION
    ↓
SIMULATION EFFECTS
    ↓
AI ACTIONS
    ↓
ENVIRONMENT UPDATE
    ↓
TURN COMPLETE
```

Later this may evolve into an energy/speed scheduler.

The first prototype should avoid over-engineering initiative.

---

# 9. Command system

UI/input must translate user intent into commands.

Example:

```rust
pub enum GameCommand {
    Move(Direction),
    Wait,
    Melee(EntityId),
    UseItem(ItemInstanceId),
    UseAbility {
        ability: AbilityId,
        target: TargetSpec,
    },
    Interact(GridPos),
}
```

The simulation validates commands.

Rendering/input must not directly mutate game state.

---

# 10. Game events

Simulation should emit events useful for:

- render effects;
- combat log;
- audio;
- scripts;
- tests.

Example:

```rust
pub enum GameEvent {
    EntityMoved {
        entity: EntityId,
        from: GridPos,
        to: GridPos,
    },
    DamageApplied {
        source: Option<EntityId>,
        target: EntityId,
        amount: i32,
        damage_type: DamageType,
    },
    ExplosionStarted {
        origin: GridPos,
        radius: u8,
        effect_id: EffectId,
    },
    TileIgnited {
        pos: GridPos,
    },
    EntityDied {
        entity: EntityId,
    },
}
```

Important:

A `GameEvent` describes what happened.

It does not contain Macroquad particles or textures.

---

# 11. Deterministic RNG

Use a deterministic seedable RNG owned by game state.

Requirements:

- world generation deterministic for a given seed;
- simulation random decisions use controlled RNG;
- tests can provide known seeds;
- save files preserve necessary RNG state or enough information to resume deterministically where appropriate.

Do not use arbitrary global randomness in simulation code.

Rendering-only randomness may use a separate non-gameplay RNG.

---

# 12. Tile model

Initial tile model must be extensible but not overbuilt.

Example conceptual fields:

```rust
pub struct TileState {
    pub terrain: TerrainId,
    pub fire: u8,
    pub liquid: Option<LiquidState>,
    pub gas: Option<GasState>,
    pub temperature: i16,
}
```

Not every field must ship in prototype.

Prefer optional components/state structures if a field is expensive or rare.

Terrain definition should carry static properties:

```rust
pub struct TerrainDefinition {
    pub id: TerrainId,
    pub blocks_movement: bool,
    pub blocks_vision: bool,
    pub destructible: bool,
    pub flammability: u8,
    pub conductivity: u8,
    pub blast_resistance: u16,
    pub sprite: AssetId,
}
```

---

# 13. Generic propagation system

Do not implement explosion, fire, electricity and gas as four unrelated algorithms.

Create a reusable propagation primitive.

Input example:

```rust
pub struct PropagationRequest {
    pub origin: GridPos,
    pub max_cost: u16,
    pub neighbor_mode: NeighborMode,
    pub policy: PropagationPolicyId,
}
```

Result:

```rust
pub struct PropagationCell {
    pub pos: GridPos,
    pub cost: u16,
    pub step: u16,
}
```

Use BFS / Dijkstra depending on cost model.

A propagation policy may determine traversal cost based on:

- terrain;
- material;
- door state;
- liquid;
- conductivity;
- custom effect rules.

Use cases:

- explosion wave;
- fire;
- electricity;
- gas;
- corruption;
- cold;
- sound.

---

# 14. Delayed visual propagation

Gameplay and rendering timing must be decoupled.

Example:

Simulation computes:

```text
origin step 0
distance 1 step 1
distance 2 step 2
distance 3 step 3
```

Presentation may animate those steps over real time:

```text
step 0 -> 0 ms
step 1 -> 50 ms
step 2 -> 100 ms
step 3 -> 150 ms
```

If the player disables animations or speeds them up, game logic must remain identical.

---

# 15. Combat data

Use explicit damage packets.

Example:

```rust
pub struct DamagePacket {
    pub source: Option<EntityId>,
    pub amount: i32,
    pub damage_type: DamageType,
    pub penetration: i32,
}
```

Keep resistance rules centralized.

Avoid each weapon manually implementing damage resolution.

---

# 16. Target model

Abilities must share a common targeting model.

Examples:

```rust
pub enum TargetSpec {
    SelfTarget,
    Entity(EntityId),
    Tile(GridPos),
    Direction(Direction),
}
```

Ability definitions can declare target type:

- self;
- entity;
- tile;
- line;
- cone;
- radius.

---

# 17. Status effects

Status effects should be data-driven where possible.

Runtime status instance:

```rust
pub struct StatusInstance {
    pub definition: StatusId,
    pub stacks: u16,
    pub remaining_turns: Option<u16>,
    pub source: Option<EntityId>,
}
```

Hooks planned:

- turn start;
- turn end;
- damage received;
- damage dealt;
- movement;
- death.

Do not create arbitrary hard-coded fields for every status.

---

# 17.5. Experience and level progression

The game requires run-local experience and level progression.

Keep these progression layers separate:

```text
META PROGRESSION
    -> unlocked starting classes / protocols and horizontal options

RUN PROGRESSION
    -> experience, levels, skills and stable core growth

MATERIAL ADAPTATION
    -> improvements to the same main body, equipment and replaceable modules

SOCIAL PROGRESSION
    -> faction reputation and individual relationships
```

Experience sources must support multiple play styles. Valid sources may include:

- defeating an appropriate threat;
- first-time exploration of a zone or important location;
- quest and objective completion;
- significant hacking outcomes;
- meaningful systemic interactions;
- peaceful, diplomatic or stealth resolutions;
- narrative discoveries;
- faction-related accomplishments;
- reaching a new simulation layer.

Do not make enemy kills the only practical source of experience.

Level rewards may include:

- skill points;
- new upgrade choices;
- occasional attribute points;
- core or ability improvements.

Do not hard-code the final experience curve or reward cadence before playtesting. Thresholds and standard reward values should be centralized and data-driven where practical.

Requirements:

- experience awards are deterministic simulation outcomes;
- the simulation emits explicit experience-awarded and level-gained events;
- one-time rewards use stable keys and cannot be granted repeatedly;
- trivial threats grant reduced or no experience;
- summoned, fabricated or infinitely reproducible entities cannot become an experience farm;
- level must not be a mandatory gate for entering a simulation layer;
- death resets run experience and level;
- meta unlocks, including unlocked starting classes, are stored separately from run progression.

Rendering and UI must never calculate or mutate experience directly.

Detailed design work is tracked in [Stats and skills: shared rules](docs/STATISTIQUES_ET_COMPETENCES.md) and the [skill catalogue](docs/PROPOSITION_COMPETENCES_v0.1.md). Their confirmed decisions, proposals awaiting approval and provisional tuning values have distinct statuses. The current documentation scope is the existing attribute and skill system; independent abilities unlocked in the world are deferred until much later and are not a prerequisite for this work.

---

# 17.6. Main body continuity and optional enemy control

Scope update: the user has deferred independent unlockable abilities until much later. Retain the notes below for continuity; do not expand their design as part of the current stats and skill documentation.

User clarification, 10 September 2026: the player's main body remains the same throughout a run. Equipment and improvements may change; permanent body swapping is not part of the progression design. Learned core abilities persist across equipment changes, while their usability may require compatible equipment. This does not change run death/restart rules.

A future optional ability may temporarily place an enemy under direct player control. It is unlocked in the game world, outside the existing skill trees; its name and acquisition method are not decided. During the effect, the main body stays at its location, present but switched off and inactive. The intended normal end of the effect returns control to the main body; interruption and destruction cases are unresolved. This is neither a permanent body replacement nor a resurrection mechanic, and it does not add a rank or choice to an existing tree.

User-confirmed rule: the inactive main body remains vulnerable and can take damage during control. Placing it in shelter before activation is a tactical choice; being switched off grants no automatic protection.

Before implementing this ability, define:

- eligible targets, resistance and unlock requirements;
- duration, costs, range/link requirements and voluntary or forced termination;
- whether damage to the main body interrupts control, and the consequences of its destruction;
- consequences of the controlled enemy's death;
- available actions and the source of effective attributes, resources and abilities;
- the perception origin during control, preserving wall occlusion and explicit information boundaries rather than implicitly merging both actors' views;
- experience attribution and faction consequences.

Do not infer invulnerability, invisibility or immunity from the word "inactive". The ability is a future design option, separate from ordinary drone orders or device hacking; it is not specified as implemented by this document.

---

# 18. Content architecture

The runtime must not repeatedly parse raw content files.

Pipeline:

```text
DISCOVER PACKAGES
    ↓
READ MANIFESTS
    ↓
RESOLVE DEPENDENCIES
    ↓
LOAD RAW DEFINITIONS
    ↓
MERGE / OVERRIDE
    ↓
VALIDATE
    ↓
BUILD RUNTIME REGISTRY
```

Content validation must fail with human-readable diagnostics.

---

# 19. Core content package

Recommended filesystem:

```text
content/
└── core/
    ├── manifest.toml
    ├── actors/
    ├── items/
    ├── weapons/
    ├── abilities/
    ├── status/
    ├── terrain/
    ├── biomes/
    ├── factions/
    ├── loot/
    ├── localization/
    ├── sprites/
    ├── audio/
    └── scripts/
```

The loader should treat this as package `core`.

---

# 20. Local mods

Default user mods directory concept:

```text
mods/
├── enabled_mod_1/
│   ├── manifest.toml
│   └── ...
└── enabled_mod_2/
```

Actual Steam/user-data path must be decided per platform.

Do not hard-code Windows-only paths into simulation/content logic.

---

# 21. Mod IDs and content namespaces

Package IDs must be globally unique enough for normal community use.

Example:

```toml
id = "alice.expanded_arsenal"
```

Content IDs:

```text
core:plasma_rifle
alice.expanded_arsenal:plasma_rifle
```

Internal representation should parse namespaces into typed IDs instead of treating them as unrelated strings everywhere.

---

# 22. Manifest format

Use TOML for package manifests unless later requirements invalidate it.

Example:

```toml
id = "alice.expanded_arsenal"
name = "Expanded Arsenal"
version = "1.2.0"
author = "Alice"

game_version = ">=0.8.0"

dependencies = [
    "core >=0.8.0"
]

optional_dependencies = []
incompatible = []
```

Use semantic version parsing rather than manual string comparison.

---

# 23. Definition file format

The exact format may be JSON5, RON, TOML or another human-editable structured format.

Before committing, prototype at least:

- one weapon;
- one actor;
- one biome;
- one ability.

Selection criteria:

- readable;
- comments desirable;
- good serde support;
- useful error locations;
- easy for non-programmers;
- stable tooling.

Do not use a binary-only format for moddable definitions.

---

# 24. Example weapon definition

Target concept:

```yaml
id: core:hellfire_m12
name_key: weapon.hellfire_m12.name
description_key: weapon.hellfire_m12.description

category: shotgun

damage:
  type: kinetic
  amount: 14

attack:
  mode: projectile
  pellets: 8
  range: 7
  spread: 3

effects:
  - type: knockback
    distance: 2

  - type: apply_status
    status: core:bleeding
    chance: 0.25
```

This is conceptual schema, not final syntax.

---

# 25. Reusable effect primitives

Create a library of generic effect primitives so most abilities require no scripts.

Examples:

- deal_damage;
- heal;
- apply_status;
- remove_status;
- knockback;
- pull;
- teleport;
- spawn_entity;
- destroy_tile;
- change_terrain;
- ignite;
- extinguish;
- propagate;
- create_zone;
- modify_energy;
- hack;
- reveal;
- play_game_event.

An ability definition is preferably a composition of primitives.

---

# 26. Scripting

Advanced mod scripting is planned.

Candidate: Rhai.

Do not implement scripting before the core event and content model is stable.

When implemented:

- scripts run in sandboxed engine;
- no arbitrary OS APIs;
- no arbitrary filesystem access;
- expose a narrow game API;
- enforce execution limits where possible;
- catch script errors;
- disable only the failing hook/mod when feasible;
- report clear diagnostics.

Potential hooks:

```text
on_spawn
on_turn_start
on_turn_end
on_hit
on_damage
on_death
on_enter_tile
on_leave_tile
on_use
on_hack
on_zone_enter
```

---

# 27. Steam Workshop

Workshop integration is a later platform layer.

The modding system must work without Steam.

Required separation:

```text
MOD SYSTEM
  ^
  |
LOCAL SOURCE
WORKSHOP SOURCE
```

Steam should provide installed package directories and metadata.

The content system should not care whether a package came from:

- core;
- local disk;
- Steam Workshop.

Planned features:

- subscribe/download awareness;
- installed Workshop mod discovery;
- enable/disable;
- dependency display;
- load order;
- Workshop item ID stored in manifest/cache;
- missing-mod diagnostics in saves.

---

# 28. Save format

Save must include at minimum:

```text
save_format_version
game_version
run_seed
simulation state
player state
loaded mod list
mod versions
```

Use stable serialized IDs.

Never serialize direct pointers/references.

Design migrations.

Save loader behavior:

- exact compatible set -> load;
- missing mod -> warn;
- incompatible version -> warn or block;
- unknown prototype ID -> fail clearly or use explicit migration/fallback;
- never silently substitute unrelated content.

---

# 29. Save migrations

Store `save_format_version`.

Migration structure:

```text
v1 -> v2
v2 -> v3
v3 -> v4
```

Do not attempt a single giant migration from all historical versions.

Early prototypes may break saves, but once public builds exist, migrations become a product requirement.

---

# 30. Localization

Do not embed player-facing strings in simulation code.

Use localization keys.

Example:

```text
weapon.hellfire_m12.name
weapon.hellfire_m12.description
status.burning.name
```

Core languages initially planned:

- French;
- English.

Mods may ship language files.

Fallback language: English unless product decision changes.

---

# 31. Rendering

Rendering target:

- top-down tile map;
- simple pixel art;
- initial candidate tile size: 32x32;
- nearest-neighbor texture filtering;
- logical camera independent from simulation.

Rendering layers example:

```text
floor
decals
terrain
items
actors
projectiles
particles
lighting
screen effects
UI
```

---

# 32. Animation system

Avoid large sprite animation dependency.

Provide generic animation primitives:

- translation;
- scale;
- rotation;
- tint/flash;
- recoil;
- shake;
- fade;
- arc movement;
- particle emission;
- screen shake;
- screen flash.

A game event can spawn an animation sequence.

Animation must be skippable/acceleratable without altering simulation.

---

# 33. Particle system

Implement a simple CPU-managed particle system first.

Particle fields may include:

```rust
position
velocity
lifetime
size
rotation
sprite/shape
```

Avoid premature GPU particle implementation.

Profile first.

Common emitters:

- impact;
- explosion;
- smoke;
- fire;
- sparks;
- electricity;
- debris.

---

# 34. UI

Initial required screens:

- main menu;
- game HUD;
- inventory;
- inspect panel;
- combat/event log;
- pause/options;
- death screen.

Later:

- build/progression;
- map;
- hacking;
- faction screen;
- mod manager.

Use an internal UI state model.

Do not allow UI code to mutate simulation objects directly.

---

# 35. Input

Support both keyboard and mouse.

Input mapping must be configurable.

Store actions, not raw keys, in gameplay UI logic.

Example actions:

```text
move_north
move_south
move_west
move_east
wait
confirm
cancel
inventory
inspect
map
ability_1...
```

---

# 36. Field of view

Implement a standard deterministic grid FOV algorithm.

Requirements:

- walls block vision;
- visible vs explored distinction;
- smoke support later;
- tests for common corner cases.

Do not couple FOV to renderer.

---

# 37. Pathfinding

Use A* for point-to-point pathfinding.

Use BFS/Dijkstra for:

- distance maps;
- local tactical fields;
- propagation.

Pathfinding should consume a movement-cost interface.

---

# 38. AI

Initial AI must remain simple and composable.

Suggested behavior primitives:

- idle;
- patrol;
- investigate;
- chase;
- melee_attack;
- ranged_attack;
- flee;
- alert_allies.

Do not create a huge behavior tree framework before basic gameplay proves the need.

Later behaviors:

- close doors;
- use alarms;
- seek cover;
- protect leader;
- hack;
- attack rival factions.

---

# 39. Factions

Entity definitions have faction ID.

Runtime relation model:

```text
friendly
neutral
suspicious
hostile
```

Prefer a central faction relation table.

Do not hard-code `player vs enemy`.

---

# 40. Hacking

Hacking is planned, not prototype-critical.

Architect it as normal game actions affecting networked world objects.

Objects may expose interfaces:

```text
power
security
data
control
```

Avoid a disconnected mini-game unless explicitly designed later.

---

# 41. Procedural generation

Generator must return a world representation independent from rendering.

First generator:

- rooms and corridors;
- deterministic seed;
- room count constraints;
- valid player start;
- reachable exit;
- enemy/item placement.

Add prefabs later.

Generation validation must check at least:

- start exists;
- exit exists;
- path exists where required;
- no mandatory object placed in invalid tile.

## Approved world direction — September 21, 2026

The full game needs both larger meaningful outdoor maps and territories containing multiple wilderness regions between major settlements and campaign milestones. Each layer has its own city anchor, wilderness destinations and optional branches. Do not treat the prototype's aligned city shaft or current map dimensions as the final world layout. Exact dimensions, distances and region counts remain unapproved.

Use constrained procedural generation: authored campaign requirements define mandatory sites, information, devices and dependencies; procedural generation varies compatible geography, approaches, encounters and optional content. City layouts may retain handcrafted identities.

The target architecture is a lightweight deterministic layer plan with stable site identities and reserved connections, created before detailed local maps are materialized on first visit. Reuse existing world, passage, facility and quest definitions where appropriate; the general planning contract is not yet implemented. Do not eagerly instantiate the whole atlas.

Validate physical reachability and logical prerequisites independently, including circular access requirements. Required destinations must exist without first accepting their quests. Recognize early discoveries when they satisfy the objective. Persist the plan and consequences across suspension and revisits; quest acceptance must not relocate content into previously explored regions.

These guarantees concern a viable initial world. They do not guarantee victory with any remaining resources or undo destructive player choices. Fallback information must have an authored, plausible source. Navigation validation alone does not prove difficulty or build viability.

See [World, exploration and progression](docs/MONDE_EXPLORATION_ET_PROGRESSION.md) for the approved design contract and its distinction from the current implementation.

---

# 42. Biome definitions

Biomes should configure generators rather than require bespoke code whenever possible.

Concept:

```yaml
id: core:industrial

generator:
  type: rooms_and_corridors
  room_count: [12, 20]

terrain_palette:
  floor: core:metal_floor
  wall: core:industrial_wall

population:
  - actor: core:maintenance_drone
    weight: 50
  - actor: core:security_unit
    weight: 30
```

---

# 43. Narrative content

Narrative should also be content-driven where practical.

Types:

- logs;
- terminal messages;
- events;
- encounters;
- dialogue;
- lore entries.

Do not encode every narrative event as a Rust function.

The full campaign must support a substantial, demanding winning run and optional sessions devoted to exploration and systemic experimentation. Main quests provide direction without making every local activity serve escape. Local stories should produce observable changes through bounded systems; this does not bring deferred faction or reputation systems into scope automatically.

Do not add an implicit global deadline merely because the protagonist wants to escape. Timed local situations must expose their trigger, turn budget and consequences. Long-term exploration also requires a sustainable, balanced resource loop for the supported play styles.

Measure winning-run duration separately from cumulative learning and failed attempts. No numeric duration or target win rate has been approved. Do not pad progression with repeated dialogue, mandatory grinding, empty travel or arbitrary level gates. First-attempt victory may be possible without being easy; permanent power upgrades or prior deaths must not be required.

The nine milestones in the proposed story are narrative roles, not a map count or a proven campaign length. The [narrative assessment](docs/EVALUATION_NARRATIVE.md) contains recommendations, not additional approved story canon.

The narrative corrections subsequently accepted on September 21 develop independent character motivations, distinct revelations, varied quest situations and optional local stories. Keep their proposed player-facing wording in [the narrative review manuscript](docs/TEXTES_NARRATIFS_PROPOSES.md), with stable editorial IDs and display conditions. This Markdown is not runtime content. Preserve the distinction between accepted direction, wording under review and implemented behavior; future integration must map IDs to localized content and verify the actual conditions before showing a line.

---

# 44. Core story invariant

The player is an AI trapped in a simulation by a human or human-origin system.

The truth must remain partially undefined in early implementation.

Do not hard-code story assumptions that prevent later narrative decisions.

---

# 45. Prototype milestone

The first playable prototype is complete when ALL of these are true:

- application launches;
- grid renders;
- player moves;
- turns advance;
- FOV works;
- procedural room map generates;
- at least 3 enemy AI types exist;
- melee works;
- ranged attack works;
- one radial propagation effect works;
- terrain can block propagation;
- one environmental interaction exists;
- inventory exists;
- at least 10 external data definitions load;
- malformed content produces useful error;
- a local mod can add one weapon without recompilation;
- game can restart a run with a seed.

Do not expand scope before this milestone.

---

# 46. Prototype content target

Suggested:

Actors:
- player;
- maintenance drone;
- security melee unit;
- security ranged unit;
- volatile unit;
- stationary turret.

Weapons/items:
- 3 melee;
- 4 ranged;
- healing/repair item;
- grenade;
- energy utility.

Abilities:
- pulse;
- scan;
- dash or short teleport;
- overload;
- defensive ability.

Terrain:
- floor;
- wall;
- door;
- explosive container;
- conductive floor or liquid.

---

# 47. Vertical slice milestone

Required:

- polished visual style;
- stable UI;
- 2-3 biomes;
- 20+ enemies;
- 40+ items;
- multiple viable builds;
- environmental simulation feels meaningful;
- beginning of story;
- save/load;
- settings;
- French + English;
- mod manager for local mods;
- documentation for adding a simple item;
- automated tests for core systems.

Steam Workshop may be implemented during or after this stage.

---

# 48. Testing requirements

Write unit tests for deterministic pure systems.

Priority tests:

- grid neighbors;
- FOV;
- propagation;
- damage;
- resistance;
- status duration;
- experience thresholds and multi-level gains;
- one-time experience reward deduplication;
- run progression reset versus preserved meta unlocks;
- content ID parsing;
- manifest dependency resolution;
- content validation;
- deterministic generation;
- save migration.

Use integration tests for:

- loading core content;
- loading a mod;
- overriding content;
- dependency ordering;
- starting a run.

Rendering tests are lower priority initially.

---

# 49. Content validation

Validation errors must include:

- package;
- file;
- content ID;
- field when possible;
- explanation;
- suggested fix when obvious.

Bad:

```text
failed to load mod
```

Good:

```text
[alice.expanded_arsenal]
weapons/hellfire.json5
alice.expanded_arsenal:hellfire_m12

Unknown status ID:
  core:bleedin

Did you mean:
  core:bleeding
```

---

# 50. Logging

Provide structured categories:

```text
APP
CONTENT
MOD
SAVE
GAME
RENDER
STEAM
SCRIPT
```

Development build may be verbose.

Release build logs should remain useful for bug reports.

---

# 51. Error philosophy

A broken optional mod should ideally not crash the entire application before the user can disable it.

Boot strategy:

1. load configuration;
2. discover packages;
3. validate;
4. if mod errors exist, show mod-error screen;
5. allow disabling offending package;
6. continue when possible.

Core content failure is fatal.

---

# 52. Performance philosophy

Do not prematurely optimize a turn-based game.

Priorities:

1. correctness;
2. deterministic behavior;
3. clean APIs;
4. profiling;
5. optimization.

Likely hotspots later:

- FOV;
- pathfinding;
- large propagation;
- particles;
- procedural generation;
- script execution.

---

# 53. Security model for mods

Never execute arbitrary native binaries from Workshop packages.

Data-only mods are default.

Advanced scripting must use sandboxed script engine.

Workshop content should be treated as untrusted input.

Validate:

- paths;
- file sizes where practical;
- data types;
- IDs;
- script limits.

Prevent directory traversal in asset references.

---

# 54. Asset handling

Content should reference assets by logical IDs or package-relative safe paths.

Example:

```text
sprite = "sprites/hellfire_m12.png"
```

Resolve only within the package unless explicitly referencing namespaced shared assets.

Reject:

```text
../../../../some_file
```

---

# 55. Mod override model

Define explicit override rules.

Recommended baseline:

- unique IDs add content;
- same namespaced ID from later package may override only if package declares it or uses explicit patch syntax;
- avoid accidental overrides.

Long-term consider patch operations:

```text
set
append
remove
multiply
```

Do not rely only on “last file wins” without diagnostics.

---

# 56. Load order

Resolve order from:

1. core;
2. dependencies;
3. user-selected ordering for unrelated mods.

Detect cycles.

Example error:

```text
A requires B
B requires C
C requires A
```

Block loading and explain cycle.

---

# 57. Workshop IDs

Do not use Steam Workshop numeric IDs as primary internal mod IDs.

Internal identity:

```text
alice.expanded_arsenal
```

Workshop ID is platform metadata.

This allows local development before publication and non-Steam builds.

---

# 58. Tooling for modders

Long-term required:

- `--validate-mod PATH`
- content validation report;
- example mod;
- schema documentation;
- log file;
- mod folder shortcut in UI.

Potential later tool:

```text
mod template generator
```

Do not make a full visual mod editor initially.

---

# 59. Steam independence

Game must launch without Steam for development.

Steam services are optional platform integrations.

If Steam is unavailable:

- single-player game works;
- local mods work;
- Workshop-specific UI clearly unavailable.

---

# 60. Rendering resolution

Do not commit to a fixed internal resolution until prototype.

Requirements:

- pixel-perfect sprites;
- support common desktop resolutions;
- integer scaling where feasible;
- camera/viewport adapts without changing simulation.

---

# 61. Accessibility

Architecture must allow:

- animation speed multiplier;
- reduce screen shake;
- reduce flashes;
- scalable UI/text;
- remappable input.

Presentation effects must be controlled through centralized settings.

---

# 62. Configuration

Store configuration separately from save files.

Categories:

- display;
- audio;
- input;
- accessibility;
- gameplay preferences;
- enabled mods / load order.

Do not serialize GPU handles or transient values.

---

# 63. Code style

Prefer:

- small modules;
- explicit ownership;
- typed IDs;
- immutable input / explicit mutation;
- pure functions for algorithms;
- clear error enums;
- minimal macros;
- documented public APIs.

Avoid:

- global mutable state;
- giant `GameManager`;
- giant `match` statements containing unrelated systems;
- hard-coded content IDs scattered throughout code;
- raw strings used as unvalidated IDs everywhere.

---

# 64. Dependency policy

Before adding a crate, record why it is needed.

Probable dependencies may include categories such as:

- serialization (`serde`);
- error handling;
- deterministic RNG;
- semantic versioning;
- data format parser;
- scripting later;
- Steamworks later.

Do not add large frameworks to solve small problems.

---

# 65. First implementation order

## Phase 1 — skeleton

1. Cargo project.
2. Macroquad window.
3. app state.
4. grid coordinate types.
5. map representation.
6. camera.
7. basic tile render.
8. player render.
9. input commands.
10. movement.

## Phase 2 — roguelike core

11. turn processing.
12. FOV.
13. procedural room generation.
14. entity registry.
15. basic AI.
16. melee combat.
17. ranged combat.
18. death/restart.

## Phase 3 — systemic effects

19. game event bus/list.
20. generic propagation.
21. explosion.
22. destructible object.
23. fire or electricity.
24. visual animation queue.
25. particles.
26. screen shake.

## Phase 4 — data-driven content

27. package manifest.
28. namespaced IDs.
29. content loader.
30. actor definitions.
31. item/weapon definitions.
32. validation.
33. core package converted to data.
34. local mod adds a weapon.

## Phase 5 — actual game structure

35. inventory.
36. equipment.
37. abilities.
38. statuses.
39. experience and level progression.
40. biome definitions.
41. content placement.
42. exit / next zone.
43. run seed.

## Phase 6 — UX

44. HUD.
45. inspect.
46. inventory UI.
47. event log.
48. settings.
49. key remapping.
50. localization.

## Phase 7 — persistence and mod UX

51. save/load.
52. save version.
53. mod list in saves.
54. mod manager.
55. mod error screen.
56. mod documentation.

## Phase 8 — advanced systems

Only after playtest:
- factions;
- hacking;
- scripting;
- richer environment;
- Steam;
- Workshop.

---

# 66. Definition of done for a feature

A feature is not complete only because it works once.

For core features, require:

- code compiles cleanly;
- errors handled;
- relevant tests;
- no rendering dependency in simulation unless required;
- configurable data where appropriate;
- localization for player-facing text;
- no unexplained hard-coded magic values;
- documentation of public extension points.

---

# 67. Codex behavior when modifying this project

When implementing a requested feature:

1. inspect existing architecture first;
2. reuse existing primitives;
3. do not create a parallel subsystem without justification;
4. preserve serialization compatibility when public saves exist;
5. preserve mod APIs when public modding exists;
6. update tests;
7. update documentation if schemas change;
8. mention migrations required by breaking changes.

---

# 68. Codex behavior for content creation

When asked to add standard content:

- prefer data files;
- do not modify Rust engine code unless new behavior is genuinely necessary;
- reuse generic effects;
- ensure IDs are namespaced;
- provide localization keys;
- validate all references.

Example request:

> Add 10 shotguns.

Expected result:

- 10 definitions;
- balance consistent with existing content;
- asset placeholders/references as needed;
- localization;
- tests/validation pass.

Not expected:

- 10 new Rust weapon classes.

---

# 69. Core narrative implementation guideline

Do not lock the narrative into a single explanation too early.

Store story content externally.

Maintain these truths:

- player is an AI;
- player is inside a simulation;
- confinement is intentional or inherited from an intentional confinement system;
- escape is a major objective;
- the origin/reason can remain ambiguous.

---

# 70. Final technical principle

The architecture should make this statement true:

> Adding a normal weapon, creature, ability, biome, faction, encounter or narrative log should usually require content files, not recompiling the engine.

And this statement should also become true later:

> A Workshop author can create meaningful new content without access to the Rust source code.

Those are foundational project constraints, not optional polish.

---

# ADDENDUM 0.2 — MANDATORY PLATFORM AND DISPLAY REQUIREMENTS

The following requirements are now part of the project's foundational technical constraints.

## Supported operating systems

The game MUST be designed, built and tested for:

- **Windows**
- **Linux**
- **macOS**

Cross-platform support is not a post-release feature.

Do not introduce platform assumptions into simulation, content or save code.

Requirements:

- never hard-code Windows path separators;
- use platform-appropriate user-data/config paths;
- isolate platform-specific integrations;
- local saves and local mods must work without Steam;
- Steam integration must remain behind a platform/service abstraction;
- avoid dependencies that prevent one of the three target operating systems unless explicitly approved.

## Display targets

Minimum design baseline:

```text
1920x1080
```

High-resolution target:

```text
3840x2160 (4K)
```

The game MUST additionally support ultrawide displays even when their horizontal resolution exceeds 3840.

Mandatory development/test presets include:

```text
1920x1080   16:9
2560x1440   16:9
3840x2160   16:9 / 4K
1920x1200   16:10
2560x1080   21:9
3440x1440   21:9
3840x1080   32:9
5120x1440   32:9
```

Supported aspect ratios MUST include:

```text
16:9
16:10
21:9
32:9
```

## Simulation independence

NO simulation rule may depend on:

- physical screen resolution;
- pixel viewport dimensions;
- monitor aspect ratio;
- UI scale.

Simulation coordinates remain grid/world coordinates.

Required pipeline:

```text
Grid coordinates
    -> world render coordinates
    -> camera
    -> viewport
    -> physical screen
```

## Camera

The camera system MUST:

- query the active viewport dimensions;
- support arbitrary wide viewports;
- preserve world scale;
- never stretch the rendered world;
- remain independent from simulation;
- allow an explicit ultrawide visibility policy.

Do NOT accidentally reveal unlimited additional tactical information simply because the window becomes wider.

The design must explicitly select one of these policies after prototype testing:

```text
A. extra width reveals more world
B. gameplay viewport is capped and side space is used by UI
C. hybrid solution
```

Until decided, implement the camera so all three remain technically possible.

## UI layout

Do not build the UI using fixed 1920x1080 screen coordinates.

UI requirements:

- anchors / constraints;
- scalable text;
- configurable UI scale;
- readable at 1080p;
- readable at 4K;
- valid layout at 21:9;
- valid layout at 32:9;
- no stretched panels;
- no critical controls placed at assumptions based on 16:9 only.

Centralize UI scaling and viewport metrics.

## Pixel-art rendering

The project currently targets simple top-down sprites, likely around 32x32 pixels.

Rendering requirements:

- nearest-neighbor filtering where appropriate;
- integer scaling where practical;
- no sprite distortion;
- camera resolution independent from physical monitor resolution;
- allow world rendering and UI rendering to use different scaling strategies if needed.

Do NOT permanently lock the project to a single internal resolution before prototype tests at 1080p, 1440p, 4K, 21:9 and 32:9.

## Window modes

Required desktop options:

- windowed;
- fullscreen;
- borderless fullscreen;
- resolution selection where supported;
- UI scale option.

The settings format must remain platform-independent.

## CI / release validation

Long-term CI should produce or validate:

- Windows build;
- Linux build;
- macOS build.

Before release, actual runtime testing is required on all three systems.

Compile success alone is not sufficient.

## Definition of done impact

Any rendering or UI feature is incomplete if it has only been verified at one resolution.

For major UI/render changes, verify at minimum:

```text
1920x1080
3840x2160
3440x1440
5120x1440
```

Where practical, use automated viewport/layout tests in addition to manual visual validation.
